# 凌小巧咖啡店 - 部署记录与禁止操作清单

> ⚠️ **重要：在此服务器上部署任何新项目前，必须先阅读本文档！**

---

## 服务器基本信息

- **IP**: 8.134.18.95
- **SSH 密码**: JR2904536462..
- **宝塔面板**: https://8.134.18.95:8888/c84b3814
- **宝塔账号**: hongjirun

---

## 凌小巧咖啡店 - 关键信息

### 域名
- 前台小程序：微信小程序（本地）
- 管理后台：https://admin.xianshihuodong.xyz
- 后端 API：https://api.xianshihuodong.xyz

### 后端服务
- **路径**: `/www/wwwroot/coffee-api/`
- **端口**: `3003`
- **PM2 进程名**: `coffee-api`
- **环境变量文件**: `/www/wwwroot/coffee-api/.env`

### 数据库
- **数据库名**: `xianshihuodong`
- **MySQL 用户**: `root`
- **MySQL 密码**: `123456`
- **MySQL 数据目录**: `/www/server/data/`（宝塔自带 MySQL 5.7）

### 管理后台文件
- **路径**: `/www/wwwroot/admin.xianshihuodong.xyz/`

### 管理员账号
- 用户名: `admin`
- 密码: `admin123`

### Nginx 配置
- API 配置: `/www/server/panel/vhost/nginx/api.xianshihuodong.xyz.conf`
- 管理后台配置: `/www/server/panel/vhost/nginx/admin.xianshihuodong.xyz.conf`

---

## 🚫 禁止操作清单（新项目部署时严禁触碰）

### 1. 禁止修改 MySQL root 密码
- 当前 root 密码为 `123456`，咖啡店后端直接使用 root 连接
- **修改 root 密码会导致咖啡店 API 全部 500 错误**
- 新项目请创建独立数据库用户：
  ```bash
  mysql -u root -p'123456' -e "CREATE USER '新用户'@'localhost' IDENTIFIED BY '新密码'; GRANT ALL ON 新数据库.* TO '新用户'@'localhost';"
  ```

### 2. 禁止强杀 MySQL 进程
- 严禁使用 `pkill -f mysqld` 或 `kill -9` 杀 MySQL
- 上次强杀导致 InnoDB 数据文件损坏，数据全部丢失
- 正确停止方式：`/etc/init.d/mysqld stop`

### 3. 禁止占用 3003 端口
- 3003 端口已被咖啡店后端占用
- 新项目请使用 3005 及以后的端口（3004 被停车场占用）

### 4. 禁止删除或覆盖以下目录
- `/www/wwwroot/coffee-api/` — 咖啡店后端
- `/www/wwwroot/admin.xianshihuodong.xyz/` — 咖啡店管理后台
- `/www/server/data/` — MySQL 数据目录（所有项目共用）

### 5. 禁止重装或降级宝塔 MySQL
- 当前版本：MySQL 5.7（宝塔管理）
- 重装会清空所有数据库

### 6. 禁止随意 reload/restart Nginx 配置文件
- 修改 Nginx 前先备份：`cp 原文件 原文件.bak`
- reload 前检查语法：`nginx -t`

---

## 服务器现有项目端口占用

| 端口 | 项目 | PM2 进程名 |
|------|------|-----------|
| 3003 | 凌小巧咖啡店 API | coffee-api |
| 3004 | 停车位小程序 API | parking-api |
| 其他 | 见 `pm2 status` | — |

---

## 新项目部署规范

### 部署前必做
1. 运行 `pm2 status` 确认现有进程
2. 运行 `netstat -tlnp | grep 300` 确认端口未被占用
3. 备份数据库：
   ```bash
   mysqldump -u root -p'123456' xianshihuodong > /tmp/coffee_backup_$(date +%Y%m%d).sql
   ```

### 数据库规范
- 每个新项目创建独立数据库用户，**不使用 root**
- 示例：
  ```bash
  mysql -u root -p'123456' -e "
    CREATE DATABASE 新项目db CHARACTER SET utf8mb4;
    CREATE USER '新项目_user'@'localhost' IDENTIFIED BY '独立密码';
    GRANT ALL ON 新项目db.* TO '新项目_user'@'localhost';
    FLUSH PRIVILEGES;
  "
  ```

### 端口规范
- 新项目从 **3005** 开始往后递增使用

---

## 常用维护命令

```bash
# 查看所有 PM2 进程
pm2 status

# 重启咖啡店后端
pm2 restart coffee-api

# 查看咖啡店日志
pm2 logs coffee-api --lines 50

# 测试咖啡店 API
curl https://api.xianshihuodong.xyz/api/orders/stats/dashboard

# 登录 MySQL
mysql -u root -p'123456'

# 备份咖啡店数据库
mysqldump -u root -p'123456' xianshihuodong > /tmp/coffee_backup_$(date +%Y%m%d).sql
```

---

**最后更新**: 2026-05-24  
**事故教训**: 部署停车场小程序时重置了 MySQL root 密码并强杀 MySQL 进程，导致咖啡店数据库损坏、数据全部丢失，重建耗时约 1 小时。
