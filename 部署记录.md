# 停车位小程序部署记录

## 服务器信息

- **IP**: 8.134.18.95
- **状态**: ✅ 已与咖啡店小程序完全分离（独立数据库 + 独立端口）
- **域名**: 
  - API: `parking.xianshihuodong.xyz`
  - 管理后台: `parking-admin.xianshihuodong.xyz`

## 数据库密码

### MySQL root（咖啡店小程序使用，请勿修改）
- 用户名: `root`
- 密码: `123456`

### 停车位小程序 - 独立数据库（与咖啡店完全隔离）
- **数据库名**: `parking_db_v2`
- **用户名**: `parking_v2`
- **密码**: `parking_v2_pass_2026`
- **端口**: `3005`（咖啡店用 3003，完全独立）

## 管理员账号

- 后台地址: `https://parking-admin.xianshihuodong.xyz`
- 用户名: `admin`
- 密码: `admin123456`

## 后端服务

- 端口: `3005`
- 进程管理: PM2 (`parking-api`)
- 路径: `/opt/parking/server`

### 常用命令
```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs parking-api --lines 50

# 重启服务
pm2 restart parking-api

# 测试 API
curl "https://parking.xianshihuodong.xyz/api/parking/nearby?lat=23.1291&lng=113.3228&radius=10000"
```

## Nginx 配置路径

- 主配置: `/www/server/panel/vhost/nginx/parking.xianshihuodong.xyz.conf`
- Admin配置: `/www/server/panel/vhost/nginx/parking-admin.xianshihuodong.xyz.conf`

### 重载 Nginx
```bash
/etc/init.d/nginx reload
```

## MySQL 操作

### 登录
```bash
/www/server/mysql/bin/mysql -uroot -pRoot@2025
```

### 查看表
```bash
/www/server/mysql/bin/mysql -uroot -pRoot@2025 parking_db -e "SHOW TABLES;"
```

## 部署过程中的问题记录

### 1. 数据库表缺失

**问题**: `Table 'parking_db.parking_lot' doesn't exist`

**解决**:
```sql
CREATE TABLE IF NOT EXISTS parking_lot (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  address VARCHAR(500),
  latitude DECIMAL(10,8) NOT NULL,
  longitude DECIMAL(11,8) NOT NULL,
  phone VARCHAR(50),
  total_spaces INT DEFAULT 0,
  city VARCHAR(100),
  status TINYINT DEFAULT 1,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

INSERT INTO parking_lot (name, address, latitude, longitude, city) VALUES
('天河城停车场', '广州市天河区天河路208号', 23.1320, 113.3245, '广州市'),
('正佳广场停车场', '广州市天河区天河路228号', 23.1350, 113.3270, '广州市'),
('太古汇停车场', '广州市天河区天河路383号', 23.1370, 113.3290, '广州市'),
('珠江新城停车场', '广州市天河区珠江新城', 23.1200, 113.3200, '广州市'),
('北京路停车场', '广州市越秀区北京路', 23.1100, 113.2600, '广州市');
```

### 2. admin_user 表缺失

**问题**: `Table 'parking_db.admin_user' doesn't exist`

**解决**:
```sql
CREATE TABLE IF NOT EXISTS admin_user (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(100) NOT NULL UNIQUE,
  password VARCHAR(255) NOT NULL,
  role VARCHAR(50) DEFAULT 'admin',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

然后通过 API 初始化管理员:
```bash
curl "http://localhost:3004/api/admin/init"
```

### 3. SSL 证书配置

通过宝塔面板申请 Let's Encrypt 证书，已配置强制 HTTPS。

### 4. 端口冲突

后端端口从 3000 依次改为 3001、3002、3003，最终使用 3004。

## 小程序配置

### 服务器域名
- request 合法域名: `https://parking.xianshihuodong.xyz`

### 本地开发
- 开启「不校验合法域名」用于调试

## 重要文件路径

### 后端
- 代码: `/opt/parking/server`
- 环境变量: `/opt/parking/server/.env`

### 前端
- 管理后台: `/var/www/parking-admin`

### 日志
- Nginx 错误日志: `/www/wwwlogs/parking.xianshihuodong.xyz.error.log`
- 后端日志: `/root/.pm2/logs/parking-api-*.log`

## 备份记录

- MySQL 数据目录备份: `/www/server/data_backup_20260524`
- Nginx 配置备份: `/www/server/panel/vhost/nginx/parking.xianshihuodong.xyz.conf.bak`

---

**记录时间**: 2026-05-24
