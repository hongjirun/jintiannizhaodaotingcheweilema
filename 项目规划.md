# 今天你找到停车位了吗 — 项目规划文档

> 版本：v1.2 | 日期：2026-05-21 | 补充Key/AppID/服务器配置信息

---

## 一、项目概述

打造一款**全国停车位地图查询微信小程序**，用户可在地图上查看全国所有停车位/停车场点位，点击图标弹窗显示停车场基本信息（名称、地址），并支持**一键导航**；同时配套一套**后台管理系统（Web端）**，供管理员对停车位点位进行增删改查及批量导入管理。

**核心定位**：辅助用户快速知晓某地是否有停车场，并一键导航前往，功能轻量简洁。

---

## 二、核心功能模块

### 2.1 微信小程序端（用户侧）

| 功能 | 说明 |
|------|------|
| 地图展示 | 基于腾讯地图/高德地图 SDK，将全国停车位以自定义图标渲染在地图上 |
| 点位聚合 | 缩放较小时自动聚合附近点位，显示数量气泡（参考图二效果） |
| 点击弹窗 | 点击停车位图标，底部弹出停车场简要信息（名称、地址、距离） |
| 一键导航 | 弹窗内一键调起腾讯/微信地图导航 |
| 搜索 | 支持按城市、停车场名称搜索 |
| 定位 | 自动获取用户当前位置，地图中心定位到用户所在地 |
| 附近展示 | 展示当前视野范围内的停车场点位 |

### 2.2 后台管理系统（管理员侧，Web端）

| 功能 | 说明 |
|------|------|
| 登录鉴权 | 管理员账号密码登录，JWT Token 鉴权 |
| 停车位管理 | 停车位点位列表，支持分页、搜索、筛选 |
| 新增点位 | 表单填写 + 地图选点，支持手动输入经纬度 |
| 编辑点位 | 修改停车场所有信息 |
| 删除点位 | 单条删除 / 批量删除 |
| 批量导入 | 上传 Excel/CSV 文件批量导入点位 |
| 数据导出 | 导出当前筛选结果为 Excel |
| 图片管理 | 上传停车场封面图（存储至阿里云 OSS，可选） |
| POI 数据拉取 | 通过腾讯位置服务 Place API 按城市批量抓取停车场 POI 入库 |
| 数据统计 | 停车位总数、城市分布概览 |

---

## 三、技术选型

### 3.1 前端 — 微信小程序

| 技术 | 选择 | 说明 |
|------|------|------|
| 框架 | **uni-app（Vue3）** | 一套代码，后续可扩展 H5/APP |
| 地图组件 | **腾讯位置服务小程序 SDK（qqmap-wx-jssdk）** | 与微信生态深度集成，支持海量点位聚合、原生导航 |
| UI 组件库 | **uni-ui** / **uView Plus** | 适配微信小程序 |
| 状态管理 | **Pinia** | Vue3 官方推荐 |
| 网络请求 | **uni.request 封装** / **axios** | 统一接口调用 |

### 3.2 前端 — 后台管理系统

| 技术 | 选择 | 说明 |
|------|------|------|
| 框架 | **Vue3 + Vite** | 现代前端工程化 |
| UI 组件库 | **Ant Design Vue** 或 **Element Plus** | 后台管理适用 |
| 地图选点 | **腾讯位置服务 JS API GL** | 后台选点与小程序同体系，坐标系统一（GCJ-02） |
| 表格/表单 | Ant Design Vue Table + Form | 增删改查标准组件 |
| 路由 | **Vue Router 4** | |
| 状态管理 | **Pinia** | |
| HTTP 请求 | **Axios** | |
| Excel 导入导出 | **xlsx.js** | 纯前端处理 Excel |

### 3.3 后端

| 技术 | 选择 | 说明 |
|------|------|------|
| 运行时 | **Node.js** | |
| 框架 | **Nest.js**（或 Express） | 推荐 NestJS，结构清晰，适合中大型项目 |
| 数据库 | **MySQL 8.x**（阿里云 RDS 或自建） | 停车位结构化数据，支持地理空间查询 |
| 地理索引 | MySQL `POINT` + 空间索引，或 **Redis GEO** | 支持附近搜索 |
| 缓存 | **Redis** | 热点数据缓存、接口限流 |
| 对象存储 | **阿里云 OSS** | 停车场图片存储 |
| 认证 | **JWT** | 后台管理员鉴权 |
| 接口文档 | **Swagger（OpenAPI）** | 自动生成，方便联调 |

### 3.4 基础设施

| 资源 | 说明 |
|------|------|
| 服务器 | **阿里云 ECS**（已有） |
| 域名 | **已有域名**，配置 SSL 证书（阿里云免费证书） |
| Web 服务 | **Nginx** 反向代理，前后端分离部署 |
| 进程管理 | **PM2**（Node.js 进程守护） |
| 对象存储 | **阿里云 OSS**（图片/文件存储） |
| 小程序发布 | 微信公众平台，域名配置合法 HTTPS 接口 |

### 3.5 关键配置信息（已确认）

| 配置项 | 值 | 说明 |
|--------|-----|------|
| 腾讯位置服务 Key | `YW6BZ-CJV6Q-CZG5Z-4LU7N-LH5Y3-G4BBY` | 小程序端地图 + WebService POI 拉取共用（已在腾讯控制台创建，Key名：今天你找到停车位了吗） |
| 微信小程序 AppID | `wx4afb276c24150291` | 已绑定腾讯位置服务 Key |
| 服务器 | 阿里云 ECS | 自有服务器，用于部署后端 + 后台管理系统 |
| 域名备案 | 已完成 ICP 备案 ✅ | 可直接配置微信小程序 request 合法域名 |

> ⚠️ **安全提示**：`腾讯位置服务 Key` 为敏感信息，后端调用时存入服务器**环境变量**（`.env` 文件），不可硬编码进代码仓库或前端代码中。小程序端引用该 Key 时仅用于地图展示，注意在腾讯控制台绑定 AppID 限制调用来源。

---

## 四、数据来源方案

### 4.1 腾讯位置服务 POI 数据拉取（合法合规）

腾讯位置服务提供 **Place Search API**，可按城市、关键词、类别检索 POI 数据，停车场类别代码为 `停车场`，覆盖全国数据，持续更新。

**数据拉取流程：**

```
1. 后台管理系统提供「城市导入」功能
2. 管理员输入城市名（如：广州市），点击「从腾讯POI拉取」
3. 后端调用腾讯位置服务 Place API（keyword=停车场&region=广州市）
4. 分页遍历所有结果（每页最多20条，自动翻页）
5. 去重后批量写入数据库
6. 管理员可对导入数据进行人工核查、编辑、删除
```

**腾讯 Place API 返回字段（可直接入库）：**

| 返回字段 | 对应数据库字段 | 说明 |
|----------|--------------|------|
| title | name | 停车场名称 |
| address | address | 详细地址 |
| location.lat | latitude | 纬度 |
| location.lng | longitude | 经度 |
| ad_info.city | city | 城市 |
| ad_info.province | province | 省份 |
| tel | phone | 联系电话 |
| id | poi_id | 腾讯POI唯一ID（用于去重） |

> **免费额度**：腾讯位置服务每日免费调用量充足，个人开发者可申请，初期不产生费用。

---

## 五、数据库设计（核心表）

### 5.1 停车场表 `parking_lot`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| poi_id | VARCHAR(50) | 腾讯POI ID（去重用，可为空） |
| name | VARCHAR(100) | 停车场名称 |
| address | VARCHAR(255) | 详细地址 |
| city | VARCHAR(50) | 城市 |
| province | VARCHAR(50) | 省份 |
| longitude | DECIMAL(11,8) | 经度（GCJ-02） |
| latitude | DECIMAL(10,8) | 纬度（GCJ-02） |
| location | POINT | 空间字段（用于附近查询） |
| cover_image | VARCHAR(500) | 封面图 OSS 地址（可选） |
| phone | VARCHAR(20) | 联系电话 |
| status | TINYINT | 状态：0停用 1启用 |
| data_source | VARCHAR(20) | 数据来源：tencent/manual |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.2 管理员表 `admin_user`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT PK | 主键 |
| username | VARCHAR(50) | 账号 |
| password | VARCHAR(255) | bcrypt 加密密码 |
| role | VARCHAR(20) | 角色（super/admin） |
| created_at | DATETIME | 创建时间 |

---

## 六、接口设计（核心 RESTful API）

### 小程序端接口

```
GET  /api/parking/bounds        # 按地图视口范围获取点位（sw_lat,sw_lng,ne_lat,ne_lng）
GET  /api/parking/nearby        # 获取附近停车位（lat, lng, radius，默认5km）
GET  /api/parking/:id           # 获取停车位详情（名称、地址、电话）
GET  /api/parking/search        # 搜索停车位（keyword, city）
```

### 后台管理接口

```
POST   /api/admin/login                    # 管理员登录
GET    /api/admin/parking/list             # 停车位列表（分页+筛选）
POST   /api/admin/parking                 # 手动新增停车位
PUT    /api/admin/parking/:id             # 编辑停车位
DELETE /api/admin/parking/:id             # 删除停车位
DELETE /api/admin/parking/batch           # 批量删除
POST   /api/admin/parking/import/excel    # Excel 批量导入
POST   /api/admin/parking/import/poi      # 从腾讯POI按城市批量导入
GET    /api/admin/parking/export          # 导出 Excel
POST   /api/admin/upload/image            # 上传图片至 OSS
```

---

## 七、项目目录结构规划

```
今天你找到停车位了吗/
├── miniprogram/              # 微信小程序（uni-app）
│   ├── src/
│   │   ├── pages/
│   │   │   ├── index/        # 地图主页
│   │   │   ├── detail/       # 停车场详情页
│   │   │   └── search/       # 搜索页
│   │   ├── components/       # 公共组件（弹窗、地图标记等）
│   │   ├── api/              # 接口请求封装
│   │   ├── store/            # Pinia 状态管理
│   │   └── utils/            # 工具函数
│   └── package.json
│
├── admin-web/                # 后台管理系统（Vue3）
│   ├── src/
│   │   ├── views/
│   │   │   ├── login/        # 登录页
│   │   │   ├── dashboard/    # 数据概览
│   │   │   └── parking/      # 停车位管理
│   │   ├── components/       # 公共组件（地图选点等）
│   │   ├── api/              # Axios 接口封装
│   │   ├── router/           # 路由配置
│   │   └── store/            # Pinia
│   └── package.json
│
├── server/                   # 后端服务（NestJS）
│   ├── src/
│   │   ├── modules/
│   │   │   ├── auth/         # 登录鉴权模块
│   │   │   ├── parking/      # 停车位模块
│   │   │   ├── upload/       # 文件上传模块
│   │   │   └── admin/        # 管理员模块
│   │   ├── common/           # 公共工具、拦截器、守卫
│   │   └── main.ts
│   └── package.json
│
└── 项目规划.md
```

---

## 八、开发阶段计划

### 阶段一：基础搭建（第 1-2 周）

- [ ] 阿里云服务器环境搭建（Node.js、MySQL、Redis、Nginx、PM2）
- [ ] SSL 证书配置，Nginx 反向代理配置（域名已备案 ✅）
- [ ] 腾讯位置服务 Key 申请（WebService Key + 小程序 Key）
- [ ] 后端 NestJS 项目初始化，数据库建表（含空间索引）
- [ ] 管理员登录接口 + JWT 鉴权
- [ ] 停车位 CRUD 基础接口开发
- [ ] 腾讯 POI 批量导入接口开发

### 阶段二：后台管理系统（第 2-3 周）

- [ ] Vue3 后台项目初始化，路由/布局搭建
- [ ] 登录页开发
- [ ] 停车位列表页（分页、搜索、筛选）
- [ ] 手动新增/编辑停车位（表单 + 腾讯地图选点）
- [ ] 删除/批量删除
- [ ] 「按城市从腾讯POI导入」功能
- [ ] Excel 批量导入/导出
- [ ] 数据统计概览页

### 阶段三：微信小程序（第 3-5 周）

- [ ] uni-app 项目初始化，腾讯位置服务小程序 SDK 接入
- [ ] 地图主页开发，停车场点位渲染 + 聚合
- [ ] 点击点位底部弹窗（停车场名称、地址、电话）
- [ ] 定位功能，自动定位到用户所在城市
- [ ] 地图拖动/缩放后自动刷新视口内点位
- [ ] 搜索功能（按名称/城市）
- [ ] 一键调起腾讯地图/微信内置地图导航

### 阶段四：联调测试 & 上线（第 5-6 周）

- [ ] 前后端联调
- [ ] 小程序微信审核，request 合法域名配置
- [ ] 性能优化（视口分块查询、Redis 缓存热点城市数据）
- [ ] 灰度测试，上线发布

---

## 九、关键注意事项

1. **腾讯位置服务 Key**：已申请 ✅，Key 为 `YW6BZ-CJV6Q-CZG5Z-4LU7N-LH5Y3-G4BBY`，在腾讯控制台已绑定小程序 AppID `wx4afb276c24150291`，WebService POI 拉取与小程序地图展示共用此 Key。后端使用时务必通过环境变量注入，**禁止明文写入代码**。
2. **域名备案**：已完成 ICP 备案 ✅，可直接配置微信小程序 request 合法域名。
3. **HTTPS**：小程序所有请求必须走 HTTPS，需配置 SSL 证书。
4. **坐标系统一**：腾讯地图使用 GCJ-02 坐标系，POI 数据导入时坐标直接可用，无需转换。
5. **海量点位性能**：按视口 bounds 查询 + 地图端 Marker Cluster 聚合，禁止一次返回全量数据。后端 MySQL 需建立空间索引（`SPATIAL INDEX`）。
6. **POI 导入频率**：腾讯 Place API 有 QPS 限制（免费5次/秒），批量导入需加速率控制，避免超额封 Key。
7. **小程序审核**：`app.json` 需声明 `scope.userLocation` 权限说明，否则定位功能审核被拒。
8. **阿里云 OSS 跨域**：如启用图片上传，OSS Bucket 需配置 CORS 规则。

---

## 十、后续可扩展功能

- 用户收藏停车场、历史查看记录
- 停车场评价/评分（用户众包补充数据质量）
- 停车场实时车位数对接（需对接停车场硬件/开放平台）
- 在线月卡购买（需接入微信支付）
- H5 网页版（uni-app 直接编译，复用代码）
- 运营数据大屏（城市覆盖率、用户访问热力图）
- 用户纠错/补充功能（用户提交新停车场，管理员审核入库）

---

*文档持续更新，开发过程中如有调整请同步修改本文档。*
